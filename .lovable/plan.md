
This is a guidance + light implementation task. The user wants three things:

1. **Google Search Console (GSC)** — verification + sitemap submission so Google indexes the site.
2. **Google Analytics 4 (GA4)** — measurement so they can see traffic.
3. **LLM discoverability** — make sure ChatGPT, Claude, Perplexity etc. surface Deepgrain.

The codebase is already in unusually good shape for this:
- `public/sitemap.xml` is auto-generated by `vite-plugins/deepgrain-seo.ts`
- `public/llms.txt` is auto-generated with full article index, foundational reading order, citation guidance
- `public/robots.txt` explicitly allows GPTBot, ClaudeBot, PerplexityBot, Googlebot, Bingbot
- Per-page `<PageMeta>` with canonical URLs, OG cards, JSON-LD on Organization + WebSite
- `CookieBanner.tsx` already exists with a `useConsent()` hook gated on `dg.consent.v1` — perfect for GDPR-compliant GA4 loading
- Custom domain is live at `https://deepgrain.ai`

So the only code change needed is **adding GA4 with consent gating**. Everything else is account setup the user does in Google's UI, plus a couple of optional LLM-discoverability boosts.

---

# Plan

## 1. Google Search Console (no code, ~5 min)

You do this in Google's UI — I can't do it for you, but here's the exact path:

1. Go to https://search.google.com/search-console
2. Add property → choose **Domain** (recommended) → enter `deepgrain.ai`
3. Google gives you a TXT record to add to your DNS (wherever `deepgrain.ai` is registered — likely Cloudflare, Namecheap, or similar). Add it, wait ~10 min, click Verify.
4. Once verified: **Sitemaps** (left nav) → submit `https://deepgrain.ai/sitemap.xml`
5. **URL Inspection** → paste `https://deepgrain.ai/` → "Request indexing". Repeat for `/method`, `/work`, `/intelligence`.

The sitemap is already live and lists every article + category, so GSC will discover the whole site within a few days.

## 2. Google Analytics 4 (small code change, consent-gated)

You set up the account; I wire in the tag.

**You do (2 min):**
1. https://analytics.google.com → Admin → Create property → "Deepgrain"
2. Add a Web data stream for `https://deepgrain.ai`
3. Copy the **Measurement ID** (looks like `G-XXXXXXXXXX`)
4. Paste it back to me

**I do, once you give me the ID:**
- Create `src/lib/analytics.ts` — a tiny GA4 loader that:
  - Only injects the gtag script *after* the user clicks "Accept" in the existing `CookieBanner`
  - Reuses the `useConsent()` hook already in `src/components/compliance/CookieBanner.tsx`
  - Sends a `page_view` on every React Router navigation (SPAs don't fire it automatically)
- Mount a `<Analytics />` component in `src/App.tsx` so it listens to route changes
- Store `G-XXXXXXXXXX` in a single constant (it's a public ID, safe to commit)
- Also wire it into GSC: GA4 → Admin → Product links → Search Console (gives you query data inside GA)

This keeps you GDPR-clean — no analytics fire until consent is granted, matching the promise already made on the cookie banner.

## 3. Maximise LLM / AI-search discoverability

Most of this is already done. What I'd add:

**Already in place (no action):**
- `public/llms.txt` — Anthropic, OpenAI and Perplexity all read this; yours is excellent (foundational reading order, citation block, full article index)
- `robots.txt` explicitly allows GPTBot, ClaudeBot, PerplexityBot
- Article JSON-LD with author, datePublished, canonical URL
- Clean canonical URLs on every page

**Worth adding (I can do these in the implementation pass):**
- **`llms-full.txt`** — same as `llms.txt` but with the full body of every article inlined. Some crawlers prefer one file over crawling N pages. Auto-generate it from the same MDX source the existing plugin already reads.
- **`Article` JSON-LD on each MDX page** — confirm it's emitted (need to check `IntelligenceArticle.tsx`); if missing, add it. This is the single biggest signal for AI citation.
- **Bing Webmaster Tools** — same idea as GSC but for Bing/Copilot. Free, takes 2 min, you can import the GSC verification.
- **IndexNow ping** — one-line POST to `api.indexnow.org` whenever the sitemap changes; Bing/Copilot index within hours instead of weeks.
- **Submit to Perplexity** — they have a publisher form at perplexity.ai/hub; not automated but worth doing once.

## What I need from you to start coding

- Your **GA4 Measurement ID** (`G-XXXXXXXXXX`) — paste it in chat
- Confirmation you want me to also build `llms-full.txt` + verify Article JSON-LD on MDX pages

Once you reply with the ID, I switch to default mode and ship the GA4 + LLM discoverability changes in one pass.
