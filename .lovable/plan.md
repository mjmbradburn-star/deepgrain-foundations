\n# Close the People Ops cluster + open new discovery surfaces\n\nTwo layers of work. **Layer 1** finishes what's outstanding (content + indexes). **Layer 2** adds the surfaces that actually move Google rankings and LLM citations once the content is in.\n\n---\n\n## Layer 1: finish the cluster\n\n### 1. Write the 5 remaining articles\n\nDerived from `docs/vault/manifest.json`. Same MDX shape as the other 18: frontmatter (title, slug, category, primaryCluster, clusters, track, description, keywords, readTime, publishedAt, author), `faqs` export, lede `<Takeaway>`, H2/H3 body, internal links, and a \