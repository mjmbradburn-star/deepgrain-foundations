## Recommendation: remove `/intelligence/people-ops`\n\nThe page duplicates `/brain` (same title, same audience, same framing) and the dual-track split adds navigation weight without a reader benefit. Cleanest move: retire the page, fold the 11 People Ops articles into the single Intelligence stream, and let `/brain` be the canonical destination for that audience.\n\nThe People Ops articles themselves stay — they're good. Only the track-level landing page goes.\n\n### What changes\n\n**1. Delete the page and route**\n- Remove `src/pages/PeopleOps.tsx`\n- Remove the lazy import + `<Route path=\